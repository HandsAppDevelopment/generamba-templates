import XCTest
@testable import {{ module_info.product_module_name  }}

class {{ module_info.name }}PresenterTests: XCTestCase {

    // MARK: - Properties

    private var presenter: {{ module_info.name }}Presenter!

    // MARK: - Lifecycle

    override func setUp() {
        super.setUp()

        let router = {{ module_info.name }}RouterMock()
        let output = {{ module_info.name }}ModuleOutputMock()
        presenter = {{ module_info.name }}Presenter(router: router, output: output)

        let view = {{ module_info.name }}ViewControllerMock()
        presenter.view = view
    }

    // MARK: - Tests

}