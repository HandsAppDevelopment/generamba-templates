import UIKit

protocol {{ module_info.name }}ViewInput: AnyObject {}

protocol {{ module_info.name }}ViewOutput {}

class {{ module_info.name }}ViewController: UIViewController, ScreenTransitionable {

    // MARK: - Properties

    let output: {{ module_info.name }}ViewOutput

    // MARK: - Lifecycle

    init(output: {{ module_info.name }}ViewOutput) {
        self.output = output
        super.init(nibName: nil, bundle: nil)
    }

    required init?(coder _: NSCoder) {
        fatalError("init(coder:) has not been implemented")
    }

    override func viewDidLoad() {
        super.viewDidLoad()
    }

    // MARK: - Prepare View

    // MARK: - Actions
}

// MARK: - {{ module_info.name }}ViewInput

extension {{ module_info.name }}ViewController: {{ module_info.name }}ViewInput {}
