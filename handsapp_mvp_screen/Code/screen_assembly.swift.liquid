import UIKit

class {{ module_info.name }}ScreenAssembly {

    func assemble(output: {{ module_info.name }}ScreenOutput? = nil) -> {{ module_info.name }}ViewController {

        let router = {{ module_info.name }}Router()
        let presenter = {{ module_info.name }}Presenter(router: router)
        let view = {{ module_info.name }}ViewController(output: presenter)
        presenter.view = view
        presenter.output = output
        router.view = view

        return view
    }
}
